# Default owner for everything in the repo.
# Using an individual rather than a team: an unresolvable team silently disables
# the whole file. Add @LuxorLabs/<team> here once the team exists.
*                       @camcalaquian

# Release-critical paths. A change to any of these can publish to npm or move the
# MCP Registry entry, so they warrant an explicit look even when the rest is not.
/.github/workflows/     @camcalaquian
/package.json           @camcalaquian
/server.json            @camcalaquian
/scripts/               @camcalaquian
